#version 430 core

in vec4 position;
out vec4 vs_color;

uniform mat4 mv_matrix;
uniform mat4 camera_matrix;
uniform mat4 proj_matrix;

void main(void) {
    gl_Position = proj_matrix * camera_matrix * mv_matrix * position;
    vs_color = vec4(1.0, 0.0, 1.0, 0.0);
}
